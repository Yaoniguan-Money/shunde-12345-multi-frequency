# ACCEPTANCE.md

## Hard-install acceptance
See master plan Section 17. Codex must turn every checkbox into evidence, not prose.

Required evidence examples:
- command + exit code for lint/type/test/build
- screenshot/log or JSON of gazetteer health
- actual imported batch ID from real file smoke test
- actual local model structured JSON output
- benchmark artifact path
- Git commit hash and remote state

A feature with only UI but no real backend behavior is not accepted.
