# 2026-08-16 工作日志

## 演讲稿对齐总升级计划实施

### WP0 冻结术语、ADR 和接口 — DONE

更新了 5 份核心文档对齐锁定决策：
- PRODUCT_SCOPE.md：全批次研判、任务前 Provider Profile 选择、完整附录 A、三天三单高频、SameEvent 语义、前端删除项、性能目标
- ARCHITECTURE.md：新数据流（understanding.v3）、ProviderProfileRegistry、Taxonomy、EventInstance V3、AnalysisScope、SameEventJudgeV2、FrequencyPolicy.v2
- DECISIONS.md：ADR-010/012/015/018 标记 superseded，新增 ADR-019 至 ADR-026
- TRAE_HANDOFF.md：删除 max_work_orders/selection_mode，添加 Provider Profile API、Taxonomy API、frequency_status、删除质量板块和附件编号
- CURRENT_STATE.md：本轮计划启动说明 + WP 状态表

### WP1 附录 A 目录与迁移 — DONE

实现了完整的 DB44/T 2479—2024 附录 A 版本化主数据系统：
- 领域模型：TaxonomyVersion/Node/Stats/CodeResolution/ClassificationOutcome + 锁定常量
- 端口：TaxonomyRepository / ClassificationValidator
- CSV 加载器：14/99/515/514/090499两条/13空 + 完整性校验
- 代码/路径解析器：唯一命中、090499歧义、父代码消歧、路径定位
- ORM + Alembic 迁移（b1c2d3e4f5a6）
- SQLAlchemyTaxonomyRepository：create_draft/activate/retire/tree/stats/resolve
- API routes：/taxonomies/active、/tree、/stats、/resolve、/seed
- 种子脚本：scripts/seed_taxonomy.py
- 16 个测试全部通过

真实 DB 验证：alembic upgrade head PASS；seed_taxonomy.py PASS (activated=true, 14/99/515/514/090499/13空)；pyright 0 errors；pytest 68 passed (16 taxonomy + 52 既有)。

### WP2 领域数据 — PARTIAL（核心删除完成）

已完成：
- 删除 `max_work_orders`/`selection_mode`：后端 schemas/analysis.py、api/analysis.py、domain/analysis_jobs.py、application/services/analysis_jobs.py、application/services/analysis.py、infrastructure/db/analysis.py
- 全批次研判：`select_work_orders` 返回批次全部成功工单
- 新字段：`target_work_order_count`/`processed_work_order_count`/`failed_work_order_count`/`produced_event_instance_count`
- 新状态：`completed_with_failures`；`provider_profile_id` 可选
- 前端：ImportsPage 删除研判数量输入、DashboardPage 删除质量指标板块、ClusterDetailPage 删除附件编号输入
- 69 个后端测试通过；前端 build 通过；OpenAPI schema 确认旧字段已移除

待实施：
- WorkOrder `reported_at` 字段+迁移、EventInstance V3、AnalysisScope 冻结

### WP7 前端对齐 — PARTIAL

已完成：删除研判数量输入、质量指标板块、附件编号输入；更新 API 类型
待实施：Provider 卡、分类级联、高频状态、详情证据
