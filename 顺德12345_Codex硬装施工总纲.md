# 顺德区 12345 多频工单智能研判平台
## Codex 硬装施工总纲 / 技术架构 / 开发规划 / TRAE 交接规范

**版本**：V1.0  
**技术选型快照**：2026-08-15  
**用途**：直接交给 Codex 作为项目从零搭骨架的总施工依据；Codex 完成“硬装”后，再交由 TRAE 做“软装”与内容丰富。  
**最高原则**：不为赶进度擅自降级、不用假数据伪装完成、不把目标态写成已实现、不以“先跑起来”为理由破坏架构边界。

---

# 0. 决策优先级与冲突规则

当旧产品文档、历史讨论、代码、AI 自行理解之间出现冲突时，按以下顺序执行：

1. **本施工总纲中明确写出的最新产品决策**。
2. 产品负责人已冻结的痛点/需求清单；若仓库中存在原文，必须逐字保留，不得由 AI 重新概括替换。
3. 当前政府提供的真实工单数据与真实地名知识库交接包。
4. 《12345多频工单智能研判平台 产品需求说明 V1.0（纯产品版）》作为基础业务参照。
5. 代码实现与旧 README。
6. AI 自行推断。

### 已明确覆盖旧 PRD 的一项决策
旧 PRD 设计了“待人工复核 → 确认/拆分/暂不确定”的阻塞式流程。当前方案改为：

- **AI 直接给出多频事件判定和事件组结果**，不设置“必须人工复核后结果才成立”的主流程。
- 仍必须保留**证据展示、人工纠错/拆分/覆盖、操作留痕**，人工纠错属于治理与修正能力，不是每个事件的必经步骤。
- “事件业务处理状态 / 处理流程 / 处理结果”与“AI 判定是否正确”是两套状态，不得混在一个字段中。

---

# 1. 产品一句话与真正技术问题

## 1.1 产品一句话
工作人员导入 Excel/CSV 工单后，系统自动理解每张工单的主体、地点和事件，将不同表达但实际指向同一现实事件的工单归并为多频事件，展示可核查依据，并支持事件后续处理、纠错、导出和数据生命周期管理。

## 1.2 不是“文本去重”，而是现实事件共指
核心问题不是：

> 两句话像不像？

而是：

> 两条或多条自然语言诉求，是否指向同一个现实主体、同一地点范围、同一具体问题、同一持续/复发事件？

因此系统核心是：

1. **Complaint Segmentation**：当前诉求 / 历史引用 / 部门回复 / 当前请求分段。
2. **Mention & Event Extraction**：主体、地点、行为、问题、时间等抽取。
3. **Entity Resolution / Linking**：简称、俗称、旧称、错别字、同义称呼统一到 Canonical Entity ID。
4. **Candidate Retrieval**：从十几万历史工单中召回少量候选。
5. **Reranking**：精排候选。
6. **Same-event Matching / Event Coreference**：判断是否同一现实事件。
7. **Event Aggregation**：将 pairwise 关系形成稳定事件组。

**Embedding 只负责召回与提供语义证据，绝不能作为最终裁判。**

---

# 2. 当前数据现实与架构约束

## 2.1 政府工单
当前政府数据是**十几万级** Excel，真实导入时必须以程序读取出的实际行数为准，不在代码里写死“14万”。样例字段很少，核心语义字段是：

- 序号/行号语义
- 工单编号
- 标题/主题概括
- 内容

导入层必须允许字段映射，因为不同批次表头可能不完全一致。

### 关键现实
- 不存在可靠的结构化“主体ID / 地点ID / 事件ID”可直接使用。
- 是否存在官方同事件标签目前不确定。
- 大量正文可能含“曾反映 / 再次反映 / 历史工单 / 部门回复”等拼接文本。
- 因此不能把整段正文不加区分地直接 Embedding 后当最终判断。

## 2.2 地名/主体本地知识库
现有交接包位于用户机器：

`C:\Users\Lenovo\Desktop\顺德地名库交接包`

交接说明：

1. 将 `顺德地名库` 文件夹拖入 Obsidian Vault。
2. 从 `顺德地名知识库.md` 开始查看。
3. 地名服务：

```bash
cd 地名服务
pip install -r requirements.txt
python server.py
# 在线调试：http://localhost:8000/docs
```

**注意：主项目后端不得再占用 8000 端口。默认后端端口使用 8080。**

### 重要边界
Obsidian 是**知识源 / Source of Truth**，不是实时热路径数据库。

禁止：

```text
出现一个实体 → 调一次 Obsidian
又出现一个实体 → 再调一次 Obsidian
```

正确方式：

```text
Obsidian / 地名服务
        ↓ 同步/编译
Runtime Entity Snapshot / Alias Index
        ↓
整张工单一次批量实体解析
```

---

# 3. 产品需求：Codex 必须搭好的“硬能力”

> 本节是工程合同，不取代产品负责人原本冻结的痛点清单；如果仓库中拿到那份“20条痛点原文”，Codex 必须原样保存到 `docs/PAIN_POINTS_LOCKED.md`，不得改写、重排或删减。

## 3.1 数据导入与批次管理

- 支持 `.xlsx`、`.xls`（如底层引擎可安全读取）和 `.csv`；至少 Excel + CSV 必须完成。
- 拖拽/选择文件。
- 字段自动识别 + 人工映射确认。
- 展示批次总数、成功数、异常数、原始重复数。
- 单行异常不得导致整批失败；可用行继续导入。
- 导入必须幂等，重复点击不能创建两个相同任务。
- 原始工单内容不可被 AI 覆写。
- 保存导入批次及数据源哈希，支持追踪该结果来自哪个文件。

## 3.2 工单理解

每条工单保留：

- 原始工单编号、标题、正文。
- 当前诉求片段。
- 历史诉求引用片段。
- 部门回复片段。
- 当前请求片段。
- 抽取出的主体 mention、地点 mention、事件/行为、时间（有则提取，没有则明确未识别）。
- 标准化后的实体 ID 与标准名。
- 事件标准化结果与摘要。
- AI 处理状态、模型版本、知识库版本、Prompt/Schema 版本。

一张工单可以有：

- 多主体；
- 多地点；
- 多事件；
- 指代关系（“该院”“那里”“这家店”）。

不得把“一条工单 = 一个主体 = 一个事件”写死。

## 3.3 实体归一化

必须解决：

- 标准名 / 简称 / 旧称 / 俗称；
- 同音或轻微错别字；
- 本地口语表达；
- 多个同名候选的上下文消歧；
- 多主体一次批处理。

示例：

```text
顺德人民医院 / 顺德一院 / 旧称 / 本地简称
        ↓
ENTITY_HOSPITAL_xxx
```

但不得仅凭一个模糊别名强行映射；有多个候选时必须结合上下文，低置信必须标记不确定。

## 3.4 多频事件识别

- AI 直接输出事件组，不设置强制人工复核队列。
- 同主体不同事件**绝不能因为主体相同就自动合并**。
- 语义相似但地点明显不同应降低或拒绝关联。
- 主体未识别但地点 + 事件高度一致时可形成候选，但证据中必须明确“主体未识别”。
- 事件组输出：事件 ID、事件名称、标准主体、地点、主要问题、关联工单、首次/最近时间（只有可靠数据时）、多频次数、置信度、证据。
- 关联依据必须是人能核查的语言，不能只展示 `cosine=0.92`。
- 无可靠证据的维度不要展示，禁止补造。

## 3.5 人工纠错与业务处理

人工可以：

- 将错误工单移出事件组；
- 合并两个事件组；
- 修改标准主体映射建议；
- 标记 AI 判定错误；
- 写备注。

人工结论必须形成独立审计记录，后续重跑模型不得无声覆盖。

事件业务处理另建状态机，例如：

`未处理 → 处理中 → 已处理 / 挂起 / 无需处理`

并记录：

- 处理步骤；
- 操作人；
- 时间；
- 处理说明；
- 处理结果；
- 可选佐证附件/链接接口（骨架留口）。

## 3.6 导出与删除

- 导出当前筛选范围事件与工单关系到 Excel/CSV。
- 导出至少包含事件 ID、事件名、主体、关联工单编号、数量、依据、置信度、处理状态/结果。
- 支持删除单条记录、批量记录、整个导入批次。
- 删除行为需确认并写审计记录。
- 数据级联规则必须明确，不能出现删工单后孤儿事件关系。
- 模型权重、缓存、索引可重建数据与用户原始数据分开管理。

## 3.7 人文关怀与社区治理信号（增强模块）

设计接口但不能越界：

- 不输出“心理异常概率”。
- 不以投诉次数给市民做人格/心理画像。
- 若未来数据存在稳定匿名诉求人 ID，可识别“长期未解决、办结后反复出现、近期诉求频率异常”等 **Care Signal**，交给工作人员关怀回访。
- 当前数据若无诉求人 ID，则**不得虚构个人关怀功能已可用**。
- 社区维度可在有可靠地点/时间后做 `Community Pulse`：多主体、多居民、多事件在区域内异常聚集 → 提醒人工核查，不自动做行政认定。

---

# 4. 目标技术架构

```text
┌──────────────────────────────┐
│ Excel / CSV 原始工单          │
└──────────────┬───────────────┘
               ↓
      Import + Schema Mapping
               ↓
     Complaint Segmenter
 当前诉求 / 历史引用 / 部门回复
               ↓
      Event & Mention Extractor
               ↓
┌──────────── Entity Resolver ────────────┐
│ Exact Alias → Fuzzy → Vector Candidates │
│                  → LLM Batch Disambig   │
└────────────────┬────────────────────────┘
                 ↓
      Canonical Event Representation
                 ↓
        Candidate Retrieval
  structure + dense + lexical evidence
                 ↓
              Top-K
                 ↓
             Reranker
                 ↓
       Same Event Matcher
 rules/structured signals + local LLM
                 ↓
          Event Relation Graph
                 ↓
        Union-Find / Components
                 ↓
        Multi-frequency Events
                 ↓
 Evidence / Correction / Handling / Export
```

## 4.1 非 Agent 化主干
高吞吐批处理主链路**不做“LLM 自主决定下一步”式 Agent**。原因：

- 十几万数据需要确定性、可恢复、可批处理；
- 每一层应有明确输入/输出和性能预算；
- 便于测试和定位错误；
- 避免无边界 Tool Call。

Agent/LLM 只用于：

- 模糊实体批量消歧；
- 复杂文本分段/事件抽取；
- 少量 Top-K 候选的同事件精判；
- 解释生成，但解释必须从结构化证据生成。

---

# 5. Local Knowledge / Obsidian 调用架构

## 5.1 Provider 接口

业务层只依赖：

```python
class GazetteerProvider(Protocol):
    async def health(self) -> GazetteerHealth: ...
    async def snapshot(self) -> GazetteerSnapshot: ...
    async def lookup_many(self, mentions: list[str]) -> list[EntityCandidateSet]: ...
```

不得让 Handler 直接依赖某个 Obsidian 插件、Markdown 目录或具体 HTTP endpoint。

## 5.2 Codex 接入步骤

1. 检查：`C:\Users\Lenovo\Desktop\顺德地名库交接包`。
2. 阅读交接包中的 `README.md` 和 `顺德地名知识库.md`。
3. 启动 `地名服务/server.py`。
4. 访问 `http://127.0.0.1:8000/docs` 或 `openapi.json`，**先读取真实 API Schema，再写客户端；禁止猜 endpoint**。
5. 优先生成/维护一个 typed client；可使用 `httpx + Pydantic`，如 OpenAPI 足够规范可评估自动生成客户端。
6. 做 `GazetteerApiAdapter`。
7. 做 `GazetteerSnapshotBuilder`：把标准实体和 aliases 编译成本地 Runtime Snapshot。
8. Runtime Snapshot 带 `snapshot_hash/version/built_at`。
9. 任何工单结果都记录当时使用的知识库版本。

环境变量：

```env
SHUNDE_GAZETTEER_HOME=C:\Users\Lenovo\Desktop\顺德地名库交接包
GAZETTEER_API_BASE_URL=http://127.0.0.1:8000
GAZETTEER_SNAPSHOT_PATH=./data/runtime/gazetteer.snapshot.json
```

路径不能硬编码在 Python 源码。

## 5.3 Runtime Entity Resolver

一次整文处理：

```text
全文
↓
Mention Extraction
↓
mention 去重
↓
Aho-Corasick 一次扫描已知 aliases
↓
RapidFuzz 模糊候选
↓
未解决 mention 批量 Embedding
↓
每个 mention Top-K Entity Candidates
↓
所有真正歧义 mention 一次 LLM Batch Disambiguation
↓
Entity Map
```

LLM 只从系统提供的候选集合中选，默认不允许凭空创造实体。

### 新 alias 学习
AI 发现可能的新别名时：

`candidate alias → proposal queue → 人工确认 → Obsidian/知识源更新 → snapshot rebuild`

禁止 AI 静默写入官方知识库。

---

# 6. 检索与同事件判断

## 6.1 召回

禁止 O(N²) 两两比较。

每个新事件表示：

```text
entity_ids
location signals
event_type / behavior
current_complaint_text
event_summary
```

召回组合：

1. Canonical Entity ID / 地点等结构化过滤（强证据）。
2. Qwen3-Embedding Dense Retrieval。
3. 字符 n-gram / 关键词 / 数字地址等 lexical evidence。
4. 可选稀疏模型作为实验组。

初期目标：Top-50/Top-20 召回，实际 K 由 benchmark 校准。

## 6.2 Reranker
对第一阶段候选精排，再缩到 Top 3~5 进入昂贵判断。

## 6.3 SameEventMatcher
输出结构化结果：

```json
{
  "same_event": true,
  "confidence": 0.93,
  "evidence": {
    "same_entity": true,
    "same_location": true,
    "same_issue": true,
    "time_compatible": null,
    "contradictions": []
  }
}
```

“同主体”不是充分条件；矛盾信号必须能否决。

## 6.4 Event Graph
不要用 HDBSCAN 作为最终业务裁决器。

- 节点：work-order event instance。
- 边：经过 SameEventMatcher 确认的 same-event relation。
- 聚合：Union-Find / Connected Components。
- 必须避免简单“传递闭包污染”：当 A≈B、B≈C、A 与 C 有硬冲突时，不得盲目合并；Event Aggregator 需支持 cluster-level consistency check。

---

# 7. 技术栈选型（2026-08-15 快照）

原则：**用新且成熟的工具解决真实问题；不因为“新”而增加无用基础设施。每个模型和检索器必须用本地真实数据 A/B，而不是只看榜单。**

| 层 | 默认选型 | 说明 |
|---|---|---|
| Python | Python 3.12 | 兼顾现代 typing 与 AI/推理生态兼容性；由 lockfile 固定 |
| Python 包/项目管理 | **uv** | 统一环境、lock、脚本；替代 pip+requirements 漂移 |
| Backend API | FastAPI + Pydantic v2 | typed API / schema / OpenAPI |
| ORM/迁移 | SQLAlchemy 2 + Alembic | 明确 repository boundary，迁移可审计 |
| Excel/CSV | **Polars + fastexcel/calamine** | 大表导入快；openpyxl 只作为格式兼容 fallback，不作为主解析路径 |
| 主库 | PostgreSQL + pgvector | 十几万规模足够，减少单独 Vector DB 运维 |
| 精确 Alias | **ahocorasick_rs** | Rust Aho-Corasick，一次扫描多别名 |
| 模糊匹配 | RapidFuzz | 高性能字符串候选 |
| Embedding | **Qwen3-Embedding 0.6B / 4B A/B** | 中文、多语、长文本；0.6B吞吐基线，4B质量候选 |
| Reranker | **Qwen3-Reranker 0.6B / 4B A/B** | 真实工单 benchmark 后锁定，不预设“大模型必胜” |
| 本地 LLM 实用档 | **Qwen3.5-9B** | 单机更现实；复杂抽取/精判主候选 |
| 本地 LLM 高质量实验档 | **Qwen3.8-27B** | 2026-08 最新开放权重候选；硬件不够则不得假装已部署，只作为 benchmark profile |
| LLM Serving | **vLLM**（WSL2/Linux/NVIDIA） | OpenAI-compatible + structured output；SGLang 作为可替换 adapter |
| Agent 边界 | PydanticAI（仅有限 LLM agent/tool 层） | typed deps/output；不接管批处理主干 |
| Job | PostgreSQL persisted job + worker | 第一版不强上 Redis/Celery；先保证可恢复和幂等 |
| Frontend | React 19 + TypeScript + **Vite 8** | Vite 8 / Rolldown；现代前端骨架 |
| Server State | TanStack Query | 统一缓存、轮询、失效 |
| UI | shadcn/ui + Tailwind CSS v4 | 组件可控，不锁死黑盒 UI 库 |
| Table | TanStack Table | 十几万数据必须 server-side pagination/filter |
| Test | pytest + Vitest + Playwright | 单元/集成/E2E |
| Lint/Format | Ruff + Pyright/类型检查 | Codex 每阶段强制过门禁 |
| Observability | structlog + OpenTelemetry 本地 | 默认不向外发任何政务数据 |
| Container | Docker Compose | Postgres 与应用依赖可复现；GPU model server 可独立于主 compose |

## 7.1 为什么不直接 Qdrant / Milvus / Neo4j / Kafka
当前十几万条规模不需要为了“看起来高级”堆基础设施。

必须先证明 PostgreSQL + pgvector 成为瓶颈，才能引入独立 Vector DB。

所有基础设施替换通过 adapter/interface 完成，而不是重写业务层。

## 7.2 Windows 现实

- 主项目可以 Windows Native 开发。
- vLLM 官方高性能 GPU 服务路径优先放在 **WSL2/Linux**。
- 不允许 Codex 因 Windows 安装困难就默默把真实本地模型换成云 API。
- 如果开发机暂时没有满足模型要求的 GPU，单元测试可用 deterministic fake provider，但“真实 AI 验收”必须单独标红为未完成，直到真实本地模型跑通。

---

# 8. Handler / Service 边界

```text
backend/app/
├── api/                    # HTTP only; thin routes
├── domain/                 # pure business objects/invariants
│   ├── complaints/
│   ├── entities/
│   ├── events/
│   └── care/
├── application/
│   └── handlers/
│       ├── import_handler.py
│       ├── segment_handler.py
│       ├── entity_resolution_handler.py
│       ├── retrieval_handler.py
│       ├── event_match_handler.py
│       ├── event_cluster_handler.py
│       ├── handling_handler.py
│       └── care_signal_handler.py
├── infrastructure/
│   ├── db/
│   ├── llm/
│   ├── embeddings/
│   ├── knowledge/
│   ├── retrieval/
│   └── observability/
├── workers/
└── schemas/
```

调用原则：

```text
API Route
  ↓
Application Handler
  ↓
Domain Services / Ports
  ↓
Infrastructure Adapters
```

禁止：

- Route 直接 import SQLAlchemy model 做复杂业务；
- UI 直接调用 model server；
- Handler 直接打开 Obsidian markdown 到处 grep；
- Domain 层 import FastAPI / SQLAlchemy / vLLM SDK。

---

# 9. 数据模型最小骨架

至少建立：

- `import_batches`
- `work_orders`
- `complaint_segments`
- `entity_mentions`
- `canonical_entities`
- `entity_aliases_runtime`（可由 snapshot 重建）
- `event_instances`
- `work_order_embeddings`
- `event_candidates`
- `event_match_edges`
- `event_clusters`
- `event_cluster_members`
- `analysis_jobs`
- `analysis_runs`
- `human_corrections`
- `event_handling_records`
- `audit_logs`
- `knowledge_snapshots`

所有 AI 结果必须带：

- `model_id`
- `model_config_hash`
- `prompt/schema_version`
- `knowledge_snapshot_id`
- `pipeline_version`
- `created_at`

保证之后能回答：“为什么这次和上次结果不同？”

---

# 10. 离线 / 在线调用逻辑

## 10.1 首次历史建库（Offline）

```text
导入十几万原始工单
↓
写 raw rows + batch
↓
模板/角色分段（规则 fast path + LLM difficult path）
↓
批量 mention/event extraction
↓
批量 entity resolution
↓
批量 embedding
↓
建 pgvector index
↓
候选召回 + rerank + match
↓
Event graph / cluster
↓
持久化结果 + versions
```

必须：

- batch；
- bounded concurrency；
- checkpoint；
- crash resume；
- 每阶段进度；
- 失败行隔离；
- 不重复算已经缓存且版本相同的 embedding/LLM output。

## 10.2 新工单（Online）

```text
1 new work order
↓
segment/extract
↓
batch entity resolve
↓
1 embedding
↓
Top-K retrieval
↓
rerank
↓
LLM only on ambiguous Top 3~5
↓
update event cluster
```

不会重新跑十几万条。

---

# 11. 性能：实事求是

## 11.1 规模判断
十几万条对 PostgreSQL / pgvector / Aho-Corasick 不是大规模问题；真正成本在本地生成式 LLM。

不能现在承诺“全量多少分钟”。Codex 必须先做基准再填数字。

## 11.2 性能预算（是目标，不是已达成事实）

- Alias exact matcher：应成为毫秒级 fast path；以 benchmark 实测为准。
- Runtime Entity Resolver 无 LLM 路径：P95 目标 < 50ms/单（硬件注明）。
- 已热身 Top-K retrieval：P95 目标 < 100ms/query。
- 在线整链不走生成式 LLM：P95 目标 < 500ms。
- 走本地 LLM：目标秒级，必须按实际 GPU/model/batch 报 P50/P95，不写虚假固定 SLA。
- 全量离线：先测 1k → 10k → full，记录 docs/s、tokens/s、显存、CPU/RAM、失败率，再预测/执行全量。

## 11.3 质量指标比速度更重要
必须建立 Gold Set：

- 500~1000 对/事件簇作为第一版人工标注集。
- 正样本可从“再次反映 / 曾反映 / 原工单号 / 仍未解决”等弱标签挖候选，再人工确认。
- Hard Negative：同主体、同类别、语义很像但不同事件/不同地点。

至少测：

- Retrieval Recall@K / Hit@K；
- Pair Precision / Recall / F1；
- Cluster pairwise F1 / B-Cubed（可选）；
- Entity Resolution Accuracy；
- LLM fallback rate；
- P50/P95 latency；
- 每 1000 条成本（本地时间/显存，而非只看 API 钱）。

没有 Gold Set，不得宣传“准确率 XX%”。

---

# 12. Codex = 硬装；TRAE = 软装

## 12.1 Codex 必须完成的硬装

1. Git repo / branch / commits / remote 策略。
2. AGENTS.md + 工程合同。
3. monorepo/basic repo tree。
4. FastAPI、DB、migration、typed schemas。
5. Job 状态、幂等、恢复机制。
6. Excel/CSV 真导入。
7. Local Gazetteer Adapter + Snapshot + Alias Index。
8. Entity Resolver interface / real fast path。
9. Embedding/Reranker/LLM Provider interfaces。
10. 本地模型服务配置与 health check。
11. Retrieval / Match / Cluster interfaces 与可运行主链。
12. AI evidence schema。
13. 核心 API/OpenAPI contract。
14. 最小可用四大核心页面路由与真实 API 接线。
15. tests / benchmark harness / CI。
16. observability / logs / version trace。
17. docs / decisions / acceptance / current state。
18. TRAE handoff 文件。
19. 最后 clean git status + checkpoint commit/tag。

## 12.2 TRAE 可以做

- 页面视觉、卡片布局、动效；
- loading/error/empty 的视觉优化；
- 表格、详情、筛选的交互丰富；
- 文案与 Demo 引导；
- 基于已有 API 的图表/内容展示；
- 不改接口语义的组件拆分。

## 12.3 TRAE 不得做

- 为了页面方便删除字段或减少功能；
- Route/UI 绕过 Handler 直接碰 DB/模型；
- 把真实 pipeline 换成 mock 静态数据；
- 私自把本地模型换云 API；
- 删除失败状态、证据、审计字段；
- 删除测试来让 CI 变绿；
- 修改 domain/API contract 而不更新 ADR + test；
- 把“AI直接判定”又改成强制人工审核；
- 删除人工纠错能力。

---

# 13. Git 与“绝不能丢记录”

Codex 第一个阶段就执行 Git，不等项目写完。

## 13.1 本地

- `git init`（若不是 repo）。
- 默认分支 `main`。
- `.gitignore` 排除原始政务数据、模型权重、缓存、`.env`。
- `.gitattributes` 统一换行和大文件策略。
- 每完成一个“可验证的硬装阶段”单独 commit。
- commit message 示例：
  - `chore: bootstrap repository and engineering contract`
  - `feat: add resumable import pipeline`
  - `feat: integrate gazetteer snapshot resolver`
  - `feat: add event retrieval and match skeleton`

## 13.2 GitHub

Codex 必须执行：

```bash
gh auth status
```

若已登录：

- 创建 private repo（仓库名由当前目录/用户确认，不随意覆盖已有 repo）；
- add remote origin；
- push main；
- 验证 remote 和最新 commit。

若未登录或权限失败：

- **不能把“创建 GitHub 仓库”写成已完成**；
- 生成 `docs/GIT_HANDOFF.md`；
- 生成 `scripts/create_github_remote.ps1`；
- 在 `docs/CURRENT_STATE.md` 明确 `REMOTE_PENDING`；
- 本地 commits 仍必须完整存在。

## 13.3 TRAE 没 Git 集成也没关系

TRAE 只需要遵守 `docs/TRAE_HANDOFF.md`，在项目根工作。若不能自己 commit：

- 每个软装阶段更新 `docs/TRAE_CHANGELOG.md`；
- 记录改动文件和验证命令；
- 交回 Codex 后由 Codex review、跑 tests、commit/push。

---

# 14. 不得降级合同

以下规则写入 AGENTS.md，并视为硬要求：

1. 不得用 stub/TODO/mock 替代被要求完成的主流程。
2. mock 仅允许测试环境，必须和生产 provider 清晰隔离。
3. 不得因依赖难装，悄悄将“本地模型”换成云 API。
4. 不得因慢，直接移除 Reranker/实体归一/证据等要求；要先 benchmark、再提出变更。
5. 不得删 failing test 让 CI 变绿。
6. 不得 `except Exception: pass` 或返回假 success。
7. 不得吞掉部分失败；必须有 failed count / reason / row id。
8. 不得修改原始工单内容。
9. 不得把 AI 推断当事实字段覆盖 raw data。
10. 不得让新模型重跑自动覆盖人工纠错。
11. 不得把 target-state 文档描述成 shipped-state。
12. 架构重大改变必须新增 ADR。
13. 每个 phase 必须 runnable、可测试、可提交。
14. 若硬件/权限导致需求无法完成，要明确 `BLOCKED`，不要自行降低验收标准。

---

# 15. 推荐 Repo 结构

```text
shunde-12345-multi-frequency/
├── AGENTS.md
├── README.md
├── pyproject.toml
├── uv.lock
├── .env.example
├── .gitignore
├── .gitattributes
├── compose.yaml
├── backend/
│   ├── app/
│   │   ├── api/
│   │   ├── application/handlers/
│   │   ├── domain/
│   │   ├── infrastructure/
│   │   ├── schemas/
│   │   └── workers/
│   └── tests/
├── frontend/
├── benchmarks/
│   ├── entity_resolution/
│   ├── retrieval/
│   └── event_matching/
├── docs/
│   ├── PRODUCT_SCOPE.md
│   ├── ARCHITECTURE.md
│   ├── ENGINEERING_CONTRACT.md
│   ├── LOCAL_KNOWLEDGE_BASE.md
│   ├── PERFORMANCE_BUDGET.md
│   ├── ACCEPTANCE.md
│   ├── DECISIONS.md
│   ├── CURRENT_STATE.md
│   ├── GIT_HANDOFF.md
│   ├── TRAE_HANDOFF.md
│   └── TRAE_CHANGELOG.md
├── scripts/
│   ├── bootstrap.ps1
│   ├── bootstrap.sh
│   ├── check.ps1
│   ├── check.sh
│   ├── sync_gazetteer.ps1
│   ├── sync_gazetteer.sh
│   └── create_github_remote.ps1
└── data/
    ├── README.md
    └── runtime/          # gitignored
```

---

# 16. Codex 开发阶段规划

## Phase 0：先验证现场，不写大功能

- 读取政府 Excel schema / 行数 / sheet / 空值分布。
- 检查地名交接包真实目录。
- 启动地名服务并读取 OpenAPI。
- 检查 GPU / CUDA / WSL2 / Docker / uv / Node / Git / gh。
- 输出 `docs/CURRENT_STATE.md`。

**Gate**：事实核验完成，不猜 endpoint、不猜硬件。

## Phase 1：Repository Hardening

- Git init + base commit。
- uv / frontend workspace。
- FastAPI / React/Vite 8 skeleton。
- Postgres + pgvector compose。
- Alembic。
- CI。
- root AGENTS。
- health endpoints。

**Gate**：一键启动，lint/type/test/build 全绿。

## Phase 2：真实数据导入

- Polars Excel/CSV reader。
- field mapping。
- import batches。
- partial errors。
- idempotency。
- server-side pagination。

**Gate**：真实政府文件可读；原始字段不改；异常可追踪。

## Phase 3：地名知识硬接入

- Gazetteer adapter。
- OpenAPI contract test。
- snapshot builder。
- ahocorasick_rs alias matcher。
- RapidFuzz。
- batch resolver API。

**Gate**：同一文本多个主体一次解析；已知别名不触发 LLM。

## Phase 4：AI Provider 与结构化理解

- Local LLM provider。
- structured output schemas。
- complaint segmenter。
- multi-event extractor。
- entity disambiguation batch。
- output cache/versioning。

**Gate**：真实样本可解释、JSON schema 100% 可验证；失败不伪造。

## Phase 5：Embedding / Retrieval / Rerank

- Qwen3 embedding baseline。
- pgvector index。
- lexical/structure evidence。
- reranker。
- benchmark runner。

**Gate**：Gold Set 上 Recall@K 有真实数值；不要只看 Demo 个例。

## Phase 6：Same-event / Cluster

- same-event schema + evaluator。
- contradiction hard rules。
- LLM precision judge。
- graph + cluster consistency。

**Gate**：至少一组真实正例 + 一组 hard negative 可稳定通过。

## Phase 7：产品闭环

- 多频事件列表。
- 事件详情证据。
- AI直接判定展示。
- 人工纠错。
- 事件处理状态/过程/结果。
- 导出。
- 删除。

**Gate**：导入 → 研判 → 查看依据 → 纠错/处理 → 导出完整走通。

## Phase 8：性能与交接

- 1k / 10k / full benchmark。
- P50/P95 / throughput / GPU memory。
- 全量失败恢复演练。
- TRAE handoff。
- Git clean。
- checkpoint tag。
- push remote 或明确 REMOTE_PENDING。

---

# 17. 验收门禁

Codex 结束“硬装”前至少满足：

- [ ] Excel + CSV 真导入。
- [ ] 原始工单不可修改。
- [ ] 多主体/多事件 schema 已支持。
- [ ] 本地地名服务真实接入，不是假 adapter。
- [ ] Runtime alias snapshot，不按主体反复访问 Obsidian。
- [ ] 本地 LLM 实际 health check 和至少一条真实 inference。
- [ ] Embedding + retrieval + reranker 主链可执行。
- [ ] same-event 不是纯 cosine threshold。
- [ ] 同主体不同事件负例测试存在。
- [ ] evidence 可核查。
- [ ] AI结果与人工纠错分层。
- [ ] 事件处理流程/状态/结果可留痕。
- [ ] 导出真实可用。
- [ ] 单条/批量/批次删除规则测试。
- [ ] background job 可恢复，不重复执行。
- [ ] model / prompt / KB / pipeline version 落库。
- [ ] Ruff/type/pytest/frontend test/build 全绿。
- [ ] benchmark 可一条命令重跑。
- [ ] README 不夸大未完成能力。
- [ ] AGENTS.md 存在。
- [ ] TRAE_HANDOFF.md 存在。
- [ ] Git 有清晰 commits。
- [ ] GitHub remote 已验证，或 `REMOTE_PENDING` 明确记录。

---

# 18. 仍需通过真实数据/现场确认的事项

这些不能由 Codex 自己拍脑袋：

1. “同一事件”的最终业务定义，尤其是时间跨度与复发事件边界。
2. 是否能从工单编号可靠推导时间；不能则不应展示伪时间。
3. 政府是否有 3~5 组官方同事件样例和 hard negative。
4. 地名服务 API 的真实 endpoint/schema。
5. 现有知识库是否只含“地名”，还是还含医院/企业/学校等主体实体。
6. GPU/显存与 WSL2 是否可用，从而锁定 Qwen3.5-9B / Qwen3.8-27B profile。
7. 当前数据是否存在诉求人匿名 ID；没有则个人 Care Signal 不进入可交付功能。
8. 删除是否要求软删除、硬删除或保留审计 tombstone；比赛版建议业务记录可删除、审计记录保留最小不可变摘要，但需业务方确认。

---

# 19. 一句话给 Codex 的施工指令

> 你负责“硬装”：先核验真实数据、真实地名服务和本机能力，再建立可恢复、可测试、可追踪、可 Git 回滚的工程骨架；以实体归一 + 事件理解 + 分层检索 + 精判为核心，不允许用一个大模型包打天下，也不允许用纯相似度冒充同事件识别。所有高成本 AI 调用必须经过批处理、候选缩减和缓存；所有结果必须能追到原始工单、模型版本、知识库版本和证据。完成后把稳定接口和清晰交接文档交给 TRAE，TRAE 只能做软装，不能破坏硬边界。
